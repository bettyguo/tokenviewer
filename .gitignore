node_modules/
dist/
*.local
.DS_Store
.vite/
.claude/

# Fetched assets — regenerated by `npm run setup`. Not committed by default to
# keep the repo lean; CI and the deploy workflow run `npm run setup` first.
# To commit them for a fully offline clone, delete the two lines below.
public/tokenizers/
public/fonts/
